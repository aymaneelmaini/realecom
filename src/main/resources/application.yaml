spring:
  application:
    name: easymart
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/easymart}
    username: ${DATABASE_USER:easymart}
    password: ${DATABASE_PASSWORD:easymart}
  liquibase:
    enabled: false

logging:
  level:
    org.springframework.jdbc.core: DEBUG
    org.springframework.data.jdbc: DEBUG
    org.springframework.transaction: DEBUG

app:
  stripe:
    secretKey: ${STRIPE_SECRET_KEY}
    webhookSecret: ${STRIPE_WEBHOOK_SECRET}